@page "/redirect"
@inject NavigationManager NavManager

<GenericPageLoader Body="No se ha identificado autenticación. Redirigiendo al inicio de sesión."></GenericPageLoader>

@code {
    protected override void OnInitialized()
    {
        NavManager.NavigateTo("/Login");
    }
}